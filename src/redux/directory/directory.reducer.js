import { MAINPAGE_DATA } from "../../data";

const directoryReducer = (state = MAINPAGE_DATA, action) => {
    switch (action.type) {
        
        default:
            return state;
    }
};

export default directoryReducer;